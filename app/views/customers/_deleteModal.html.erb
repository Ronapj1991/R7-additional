<div class="modal fade" id="myModal-<%= customer.id %>" tabindex="-1" aria-labelledby="myModal-<%= customer.id %>" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="modal-title fs-5" id="exampleModalLabel">Delete</h1>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body">
            <p>Are you sure you want to delete <%= customer.full_name %>? All associated orders will be deleted too.</p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Cancel</button>
            <%= link_to "Confirm", "/customers/customerAndOrders/#{customer.id}", class: "btn btn-danger", data: { "turbo-method": :delete } %>
          </div>
        </div>
    </div>
</div>